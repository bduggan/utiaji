%| :$today, :$cal, *%args

%== include $app, 'header', |%args;

<row>
<div class="column" id='content'>
loading...
</div>
</row>

<script type='text/javascript'>
  var state = <%==  to-json($cal.initial_state) %>;
  state.first = new Date(...state.first);
  state.month = month_name(state['month_index']);
  var cal = React.createElement(Cal, {
    initial_data : state
    }
  );
  var ref = ReactDOM.render(cal, document.getElementById('content'))
</script>

%== include $app, 'footer', |%args;
