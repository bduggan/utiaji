#!/usr/bin/env perl6

use lib 'lib';

use Hamna::Server;
use Hamna::Log;
use Hamna;

logger.level = 'debug';

my $server = Hamna::Server.new(host => 'localhost', port => 3333);
$server.app = Hamna.new;
$server.start;
$server.await;

