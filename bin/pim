#!/usr/bin/env perl6

use lib 'lib';

use Uhitaji::Server;
use Uhitaji::Log;
use Uhitaji::App::Pim;

logger.level = 'debug';

my $server = Uhitaji::Server.new(host => 'localhost', port => 3333);
$server.app = Uhitaji::App::Pim.new;
$server.start;
$server.await;

