#!/usr/bin/env perl6

use lib 'lib';

use Uhitaji::Server;
use Uhitaji::Log;
use Uhitaji;

logger.level = 'debug';

my $server = Uhitaji::Server.new(host => 'localhost', port => 3333);
$server.app = Uhitaji.new;
$server.start;
$server.await;

