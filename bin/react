#!/usr/bin/env perl6

use lib 'lib';

use Uhitaji::Server;
use Uhitaji::Log;
use Uhitaji::App::React;

logger.level = 'debug';

my $server = Uhitaji::Server.new(host => 'localhost', port => 3334);
$server.app = Uhitaji::App::React.new;
$server.start;
$server.await;

