#!/usr/bin/env perl6

use lib 'lib';

use Hamna::Server;
use Hamna::Log;
use Hamna::App::React;

logger.level = 'debug';

my $server = Hamna::Server.new(host => 'localhost', port => 3334);
$server.app = Hamna::App::React.new;
$server.start;
$server.await;

