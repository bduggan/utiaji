sudo: required
env:
  - PATH=$PATH:$HOME/.rakudobrew/bin PGDATABASE=test CACHED=$HOME/build_cache PGUSER=postgres PGPORT=5433
before_install:
  - sudo /etc/init.d/postgresql stop
  - sudo ps aux|grep -i postg
  - wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
  - sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt/ precise-pgdg main 9.5" >> /etc/apt/sources.list.d/postgresql.list'
  - sudo apt-get update
  - sudo apt-get install postgresql-9.5
  - sudo /etc/init.d/postgresql start
  - sudo ps aux|grep -i postg
  - sudo find /var/run/postgresql
  - psql -V
  - psql --list
  - psql -c 'select version()'
  - createdb testing123
install:
  - ./install.sh
before_script:
  - createdb test -U postgres
  - psql test -f schema.sql
script:
  - prove -Ilib -v -r --exec=perl6 t/
cache:
  directories:
    - $HOME/.rakudobrew
